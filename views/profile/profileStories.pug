extends ../layout

include ../mixins/_storyCard

block content
  li: a(href=`/u/${user.username}/reees`) Salty Stories
  li: a(href=`/u/${user.username}/upvotes`) Upvotes
  li: a(href=`/u/${user.username}/comments`) Comments
  
  each story in stories
    +storyCard(story)