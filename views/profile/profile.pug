extends ../layout

include ../mixins/_storyCard

block content
  li: a(href=`/u/${author.slug}/reees`) Salty Stories
  li: a(href=`/u/${author.slug}/upvotes`) Upvotes
  //- li: a(href=`/u/${author.slug}/comments`) Comments
  hr
  
  if stories
    each story in stories
      +storyCard(story)
    