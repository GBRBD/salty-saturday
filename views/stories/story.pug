extends ../layout

block content
  h2= story.title
  a(href=`/p/${story.author.username}`) #{story.author.username}
  p= story.created
  //- Only the author can edit the story
  if user && story.author.equals(user._id)
    a(href=`/stories/${story._id}/edit`) Edit
  p= story.story
  //- Salty points 
  //- Comments
  //- Report
  //- Share
  //- Upvote